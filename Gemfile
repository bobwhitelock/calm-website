source "https://rubygems.org"
ruby File.read(File.join(File.dirname(__FILE__), '.ruby-version')).chomp

gem 'jekyll', '3.3.1'

# Add all Jekyll plugins here.
group :jekyll_plugins do
  gem 'jekyll-contentful-data-import'
  gem 'jekyll-cloudinary'
end
